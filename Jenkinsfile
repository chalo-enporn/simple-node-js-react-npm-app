pipeline {
    agent any
 
    stages {
            stage('Build') {
                steps {
                    echo 'Start Build -> npm install'
                    bat 'npm install'
                }
            }
            
        }
    }

 